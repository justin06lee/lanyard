{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Lanyard floater, session panel, search and permissions windows",
  "windows": ["floater", "panel", "search", "setup"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
